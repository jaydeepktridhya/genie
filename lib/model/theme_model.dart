import 'dart:ui';

class ThemeModel {
  final Color bgColor;
  final bool isPremimumVisible;
  final Color textBgColor;
  final Color textColor;

  ThemeModel(
      this.bgColor, this.isPremimumVisible, this.textBgColor, this.textColor);
}
