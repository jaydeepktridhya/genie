<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.-->

    <style name="KwKeyboard">
        <item name="itemTextAppearance">?attr/itemTextAppearance</item>
    </style>

    <style name="KwKeyboardView" parent="KwKeyboard">
        <item name="keyTextSize">@dimen/keyboard_text_size</item>
        <item name="android:background">#E3E5E5</item>
        <item name="colorAccent">@android:color/black</item>
    </style>

    <style name="MyKeyboardViewPopup" parent="KwKeyboard">
        <item name="keyTextSize">@dimen/preview_text_size</item>
    </style>

    <style name="keyBoardHeader">
        <item name="android:gravity">center</item>
        <item name="android:orientation">vertical</item>
        <item name="android:padding">4dp</item>
        <item name="android:visibility">visible</item>
        <item name="android:layout_marginEnd">18dp</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:paddingBottom">4dp</item>
    </style>

    <style name="spinnerStyle">
        <item name="android:layout_width">180dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">20dp</item>
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:elevation">0dp</item>
        <item name="android:visibility">invisible</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:popupBackground">@drawable/bg_drop_down</item>
    </style>

    <style name="progressIndicatorStyle">
        <item name="android:layout_width">32dp</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:layout_marginTop">@dimen/genie_dimen_12dp</item>
        <item name="android:layout_marginEnd">@dimen/genie_dimen_20dp</item>
        <item name="android:padding">@dimen/genie_dimen_4dp</item>
        <item name="android:paddingBottom">@dimen/genie_dimen_4dp</item>
        <item name="android:visibility">gone</item>
    </style>
</resources>
